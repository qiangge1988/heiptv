services:
  heiptv:
    build: .
    image: heiptv:v2
    container_name: heiptv
    restart: always
    ports:
      - "554:554"
      - "2000:8080"
    volumes:
      - ./config:/app/config
      - ./output:/app/output
    environment:
      - TZ=Asia/Shanghai
